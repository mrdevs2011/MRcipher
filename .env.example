# MRcipher Environment Variables
# Copy this file to .env.local and fill in your values.

# Firebase Admin SDK service account (JSON string or path to service account file)
# Preferred: paste the JSON content of your service account key here.
FIREBASE_SERVICE_ACCOUNT_JSON=

# Fallback individual Firebase Admin fields (used if FIREBASE_SERVICE_ACCOUNT_JSON is empty)
FIREBASE_PROJECT_ID=
FIREBASE_CLIENT_EMAIL=
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"

# Master 256-bit key used to encrypt all payloads. Must be exactly 64 hex chars (32 bytes).
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_MASTER_KEY=

# Optional: comma-separated global origins allowed for preflight/health checks.
# Per-key allowed domains are stored in Firestore and take precedence for actual requests.
ALLOWED_ORIGINS=

# Public URL of the deployed API. Used for documentation and CORS examples.
NEXT_PUBLIC_APP_URL=https://mrcipher.vercel.app
